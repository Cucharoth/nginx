# Define the user and worker processes
user nginx;
worker_processes auto;
pid /run/nginx.pid;

# Setup events module
events {
    worker_connections 1024;
}

http {
    # Incrementar el tiempo máximo de espera para el cliente
    client_body_timeout 3m;
    client_header_timeout 3m;
    
    # Incrementar el tiempo de espera del proxy (en caso de que Nginx esté actuando como proxy inverso)
    proxy_connect_timeout 3m;
    proxy_send_timeout 3m;
    proxy_read_timeout 3m;
    
    # Aumentar el tiempo de espera de los buffers
    send_timeout 3m;

    # Define log formats and paths
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Define the MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # HTTP Redirect (Global)
    server {
        listen 80;
        server_name asistify.cedrickirmayr.dev api-asistify.cedrickirmayr.dev;

        # Allow Certbot to verify the domain via HTTP
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Redirect all other traffic to HTTPS
        location / {
            return 301 https://$host$request_uri;
        }
    }

    # # Frontend Server (HTTPS)
    # server {
    #     listen 443 ssl;
    #     server_name asistify.cedrickirmayr.dev;

    #     # Let's Encrypt Certificates
    #     ssl_certificate /etc/letsencrypt/live/asistify.cedrickirmayr.dev/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/asistify.cedrickirmayr.dev/privkey.pem;

    #     # SSL Best Practices
    #     include /etc/nginx/snippets/ssl-params.conf;
    #     include /etc/nginx/snippets/resolver.conf;
        
    #     set $asistify_frontend "asistify-frontend:8080";

    #     location / {
    #         proxy_pass http://$asistify_frontend;
    #         include /etc/nginx/snippets/proxy_params.conf;
    #     }

    #     location = /health {
    #         access_log off;
    #         return 200 'ok';
    #         add_header Content-Type text/plain;
    #     }
    # }

    # # Backend Server (HTTPS)
    # server {
    #     listen 443 ssl;
    #     server_name api-asistify.cedrickirmayr.dev;

    #     # Let's Encrypt Certificates
    #     ssl_certificate /etc/letsencrypt/live/asistify.cedrickirmayr.dev/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/asistify.cedrickirmayr.dev/privkey.pem;

    #     # SSL Best Practices
    #     include /etc/nginx/snippets/ssl-params.conf;
    #     include /etc/nginx/snippets/resolver.conf;

    #     set $asistify_backend "backend:3000";

    #     location / {
    #         proxy_pass http://$asistify_backend;
    #         include /etc/nginx/snippets/proxy_params.conf;
    #     }
    # }
}
